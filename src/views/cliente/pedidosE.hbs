<div class="row">
    <h1>Pedidos Entregados</h1>
</div>
<div class="row" style="margin-right: 200px;">
  <table class="table table-striped">
    <thead>
      <tr>
        <th style="width: 10px">#</th>
        <th>Codigo Pedido</th>
        <th>Cliente</th>
        <th>Total Facturado</th>
        <th>Progreso de Entrega</th>
        <th style="width: 40px">Estado</th>
        <th style="width: 40px">Accion</th>
      </tr>
    </thead>
    <tbody>
      {{#each peE}}
      <tr>
        <td>{{id}}</td>
        <td>Cod-{{codVenta}}</td>
        <td>{{nombre}}</td>
        <td style="text-align: center;">${{total}}</td>
        <td>
          <div class="progress progress-xs">
            <div class="progress-bar progress-bar-danger" style="width: {{porcentaje}}%"></div>
          </div>
        </td>
        <td><span class="badge {{color}}">{{estado}}</span></td>
        <td><a class="btn btn-primary" href="http://localhost:4000/pedido/{{codVenta}}">Ver detalle</a></td>
      </tr>
      {{/each}}

    </tbody>
  </table>

  <!-- /.card-body -->

</div>